决定 DP 维数的是：状态需要几个变量来描述
问自己："我需要记录什么才能推出下一个答案？"

1D 的情况：状态只依赖一个变量
比如 lc139，dp[i] = "前 i 个字符能否拆分"，只需要知道位置 i 就够了。
pythondp[i] = dp[j] and s[j:i] in wordDict
# 虽然有 i, j 两层循环，但 j 只是用来枚举切割点
(双层 for loop 里的第二个变量如果只是用来枚举可能性，不需要记录它的状态，就不需要二维数组。)

2D 的情况：状态需要两个变量才能描述
比如 lc1143 最长公共子序列，需要同时知道 s1 走到哪 和 s2 走到哪：
pythondp[i][j] = s1[0:i] 和 s2[0:j] 的最长公共子序列长度
