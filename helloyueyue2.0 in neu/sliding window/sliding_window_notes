1.滑动窗口的语义是：window 永远代表当前区间的真实状态。
不是“理想状态”，不是“去重后的状态”，
而是：right 指针走到哪，窗口里就真实包含哪些数。
如果违反题目规定的某些要求 → 再收缩窗口

2.记得用right - left + 1记录长度，而不是重新记录curr_len

1.定长
for loop
    先扩
    如果超过长度 → 缩
    当窗口长度 == k 时update answer

2.不定长
    求最短：
    for loop
        while 合法：
            update
            缩

    求最长：
    for loop
        while 非法：
            缩
        update


